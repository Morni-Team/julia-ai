# CLAUDE.md – Leitfaden & Fehler-Journal für Julia

Diese Datei ist für Claude bzw. den Agenten, der an **Julia** (julia-ai) arbeitet.
Sie hält fest, wie hier gearbeitet wird, welche Fehler schon aufgetreten sind und
**wie** sie behoben wurden – damit derselbe Fehler nie zweimal passiert.

## Was Julia ist

Windows-Desktop-Assistent (Electron): sieht den Bildschirm, spricht lokal (Whisper/Piper),
steuert den PC und spielt Minecraft mit. Jede Aktion läuft durch die
**Ampel** (grün/gelb/rot). Details: [README.md](README.md).

> Hinweis: Die früheren Integrationen der **eigenen Apps des Nutzers** (ToDoch, Streamo,
> VibeWork/VibeWorks, Patchfeld, Codewerk, Content-Helper) wurden auf ausdrücklichen
> Wunsch **komplett entfernt** (`src/main/apps.js`, `src/main/vibeworks.js` gelöscht,
> `apps`-Werkzeug + VibeWorks-MCP-Login raus). Nicht wieder hinzufügen. Der generische
> MCP-Server-Support und die Handy-Brücke (`appserver.js`) bleiben.

## Grundregeln beim Arbeiten

- **„WAS IST WENN?"** – Immer den Fehlerfall zuerst denken. Kein Pfad darf zu einem
  wortlosen Absturz führen. `try/catch` um alles, was scheitern kann (Datei, Netz, GPU,
  fremde API); im Zweifel eine klare deutsche Meldung, nie ein stiller Abbruch.
- **Tests grün halten**: `node --test` vor jedem Release. Neue Logik = neuer Test.
- **Aus Fehlern lernen – immer ein Test dagegen** (Wunsch JONIMONI09): Nach **jedem**
  behobenen echten Bug einen **Regressions-Test** schreiben, der genau diesen Fehler
  gefangen hätte, **und** einen Fehler-Journal-Eintrag anlegen. Kein Bugfix ohne Test.
  (Beispiel #70: `test/renderer-skripte.test.js` fängt seitenweite Skript-Kollisionen –
  fand dabei gleich eine zweite, maskierte.)
- **Update-Schema** (siehe README §Updates): Version in Zehnerschritten, eine Changelog-Zeile
  in Nutzersprache, Commit mit Versionsnummer vorne, Tag, GitHub-Release, Installer anhängen –
  am einfachsten `node scripts/release.js <korrektur|funktion|bruch> "…"`.
- **„Das ist neu"** in beiden READMEs bei **jeder** Version pflegen: neuestes oben, unterstes raus.
- **Nie destruktives Git** (Historie umschreiben, Releases/Tags löschen, Repo-Sichtbarkeit)
  ohne klare Ansage.

## Datenschutz beim Logging & bei Diagnose (verbindlich)

Wenn Logs oder Crash-Berichte den PC verlassen (nur **nach ausdrücklicher Zustimmung**, opt-in):

- **NIEMALS** versenden: IP-Adressen, API-Tokens, Passwörter, Zugangsdaten, Dateiinhalte,
  Screenshots, persönliche Daten (Name, E-Mail), Pfade mit dem Benutzernamen.
- **Erlaubt** sind nur technische Diagnosewerte: Julia-Version, Windows-Version, GPU/Treiber,
  Fehlermeldung/Stacktrace (vorher von Pfaden/Namen bereinigt), ob Software-Rendering aktiv ist.
- Vor dem Senden **bereinigen** (Scrubber): Benutzernamen aus Pfaden, alles, was wie Token/
  E-Mail/IP aussieht, ersetzen. Standard ist **aus**; der Nutzer wählt es bewusst.
- Lokale Logbücher (`start.log`, Minecraft-Logbuch, PC-Steuerungs-Leistungslogbuch) bleiben auf
  dem PC und dürfen mehr enthalten; sie werden nicht automatisch versendet.
- **PC-Steuerung/Leistung (Issue #25):** Bei jeder Steuerungs-Aktion (Klick, Tippen, Screenshot,
  Programm/Fenster) hält Julia lokal **nur Technisches** fest – Aktionsname (festes Vokabular),
  Dauer und ihren **eigenen** CPU-Verbrauch (`process.cpuUsage()`, kein PowerShell → erzeugt beim
  Messen keine Last) sowie ihren Speicher. **Nie** Inhalt: kein getippter Text, keine Koordinaten,
  kein Fenstertitel, kein Screenshot-Inhalt. An den Dev geht davon nur eine **aggregierte,
  bereinigte** Zusammenfassung, und nur bei eingeschalteter Diagnose (`diagnose.senden`, Standard
  aus) – [src/main/leistung.js](src/main/leistung.js).

## Fehler-Journal

Format: **Datum · Fehler · Ursache · Fix (welche Datei)**. Neueste oben.

| Datum | Fehler | Ursache | Fix |
|---|---|---|---|
| 2026-09-24 | Julia spammt beim Durchspielen ihre Herstell-/Status-Meldungen in den **Minecraft-Spielchat** („48× oak_planks hergestellt", „crafting_table steht bei …", „16× stick hergestellt" …) – der Nutzer will das per **Ein/Aus-Knopf** steuern, sonst soll sie normal im Chat schreiben können | `_fertig(a, text)` schrieb JEDES Aufgaben-Ergebnis bedingungslos per `this.chat(text)` in den Spielchat – auch die von der KI beim autonomen Durchspielen ausgelösten Herstell-/Fortschritts-Meldungen. Im Fenster (`_melden`) standen sie ohnehin | Neuer Schalter `minecraft.chat_fortschritt` (Standard **aus**): `_fertig` schreibt die Status-/Fortschritts-Meldung nur noch in den Spielchat, wenn der Schalter an ist (`this.fortschrittInChat`); im Julia-Fenster bleibt sie immer sichtbar. Normale Antworten an Spieler (AI-Reply-Pfad, Grüße) sind davon unberührt. Setter `setFortschrittInChat`, verdrahtet in [src/main/main.js](src/main/main.js) (Initial + `config.on('aenderung')`), UI-Schalter in [src/renderer/mc.js](src/renderer/mc.js)/[chat.html](src/renderer/chat.html), Texte de/en. Mit Test – [src/main/minecraft.js](src/main/minecraft.js) `_fertig`/`setFortschrittInChat`, [src/main/config.js](src/main/config.js). Merke: Julia schreibt in den Minecraft-**Spielchat** nur zurückhaltend – Status/Erzählung bleibt standardmäßig im Fenster, Chat-Schreiben ist opt-in |
| 2026-09-24 | Nach einem Update startet Julia **gar nicht mehr** – Renderer crasht mit `-2147483645` (STATUS_BREAKPOINT) **in jedem** Grafik-Modus, auch `gpu-aus`/`in-process-gpu` (Issue #109, weiterhin gemeldet) | Diese Absturz-Signatur direkt beim Start kommt fast immer NICHT von der Grafik, sondern von einer **injizierten Fremd-DLL** (Antivirus/Overlay/Tuning-Tool) oder von **Windows-Code-Integrity**, die eine unsignierte injizierte DLL im Renderer killt. Alle bisherigen Stufen ändern nur Grafik-Flags und ließen diesen Fall unadressiert → auf der letzten Grafik-Stufe (`gpu-aus`) blieb nur FATAL, die App war unbrauchbar | Neue **allerletzte Rettungsstufe `notfall`** (nach `gpu-aus`): behält alles von `gpu-aus` und setzt zusätzlich `--no-sandbox`, `--disable-gpu-sandbox` und `--disable-features=RendererCodeIntegrity` – die **dokumentierte Windows-Mitigation** genau gegen injizierte-DLL-/Code-Integrity-Renderer-Crashes. Die Start-Absicherung eskaliert jetzt `software → gpu-aus → notfall → FATAL` (neuer reiner Helfer `grafik.naechsteRettung`); erst wenn auch der Notfall-Modus crasht, klare Meldung, dass es am **Treiber oder an Sicherheitssoftware** liegt (mit Ausweg-Knopf aus #110). Mit Tests – [src/main/grafik.js](src/main/grafik.js) (`MODI`/`flaggenFuer`/`naechsteRettung`/`hardwareAus`), [src/main/startpruefung.js](src/main/startpruefung.js) (`startAbsichern`/`ausweichen`). Merke: `-2147483645` beim Start trotz `gpu-aus` = Fremd-DLL/Code-Integrity, nicht Grafik – dagegen helfen `--no-sandbox` + `--disable-features=RendererCodeIntegrity`, kein weiteres Grafik-Flag |
| 2026-09-24 | „Wieder Grafik, obwohl es davor lief" (Issue #110): auf einem stark degradierten NVIDIA-PC crasht der **Renderer** auch im Not-Grafikmodus (`gpu-aus`/`in-process-gpu`, `code -2147483645`) → FATAL-Dialog, und der Nutzer musste **neu installieren**, um wieder reinzukommen; zusätzlich meldete die Defender-Ausnahme „UAC abgelehnt", obwohl der Nutzer auf „Ja" geklickt hatte | (1) Der wiederkehrende `-2147483645` (STATUS_BREAKPOINT) auf **allen** Chromium-Kindprozessen ist die Signatur eines kaputten Treibers / einer injizierten Fremd-DLL (Sicherheitssoftware), nicht per Grafik-Flag heilbar – der FATAL-Dialog bot bisher **keinen Ausweg außer Neuinstallation**. (2) Die Defender-Fehlerdeutung zählte jedes „Zugriff verweigert"/„denied" als UAC-Ablehnung; der Manipulationsschutz liefert aber genau so einen Fehler **trotz** geklicktem „Ja" → Fehlmeldung | (1) FATAL-Dialog bietet jetzt **„Grafik zurücksetzen & neu starten"** (setzt `grafikModus` auf `normal` und `app.relaunch()` → Fallback-Leiter startet frisch, **keine Neuinstallation** mehr nötig) und **„Grafiktreiber aktualisieren"** (öffnet die Hersteller-Treiberseite per PCI-Vendor-ID) – [src/main/startpruefung.js](src/main/startpruefung.js) `fehlerDialog`, verdrahtet in [src/main/main.js](src/main/main.js) `startFatal` (`zuruecksetzen`/`treiberUrl`). (2) Neuer reiner Helfer `fehlerArt(err)` in [src/main/defender.js](src/main/defender.js): **nur** der echte UAC-Abbruch (Windows-Fehler **1223** „durch den Benutzer abgebrochen") gilt als „UAC abgelehnt", „Zugriff verweigert" trotz Ja → Manipulationsschutz-Meldung mit genauer Klick-Anleitung. Mit Test. Merke: `-2147483645` auf allen Kindprozessen ist Treiber/Fremd-DLL – nicht mit weiteren Flags raten, sondern Ausweg-Knopf + ehrlichen Hinweis geben; und „Access denied" ≠ UAC-Abbruch (nur 1223 ist der Abbruch) |
| 2026-09-24 | Nach Update bleibt die Grafik tot – jetzt **Renderer**-Absturz im `gpu-aus`-Modus („GPU access disabled through --disable-gpu and --disable-software-rasterizer", `code -2147483645`) (Issue #109, Folgefehler zum #107-Fix) | Der #107-Fix (`--disable-software-rasterizer` in `gpu-aus`) schaltete zwar den crashenden GPU-Prozess ab, nahm dem **Renderer** damit aber JEDEN Zeichen-Pfad (kein GPU, kein SwiftShader) → Renderer stürzt sofort ab. Update selbst lief laut Log sauber durch (6.9.0→6.9.1) | `gpu-aus` nutzt jetzt **`--in-process-gpu`** statt `--disable-software-rasterizer`: GPU-/SwiftShader-Arbeit läuft im **Browser-Prozess** (kein separater, crashender GPU-Kindprozess), Software-Rendering bleibt erhalten. Zusammen mit `disableHardwareAcceleration` = rein Software, ohne GPU-Kind – [src/main/grafik.js](src/main/grafik.js). Merke: `--disable-software-rasterizer` NICHT nutzen, wenn ohnehin `--disable-gpu` gesetzt ist – sonst hat der Renderer keinen Backend und stirbt; `--in-process-gpu` ist der Weg, den GPU-Kindprozess zu vermeiden und trotzdem zu rendern |
| 2026-09-24 | Defender-Ausschluss „funktioniert einfach nicht" – roher PowerShell-Fehler „Start-Process … Zugriff verweigert" im Dialog (Issue #108) | `Start-Process -Verb RunAs` scheitert mit „Zugriff verweigert", wenn die **UAC-Abfrage abgelehnt/blockiert** wird; die rohe Fehlermeldung wurde 1:1 als `r.fehler` an die Oberfläche gereicht. Zusätzlich wurde nicht auf den elevated Vorgang gewartet und nicht verifiziert, ob der Ausschluss wirklich gesetzt wurde | `Start-Process` mit **`-Wait`**; danach per `status(pfade)` **verifizieren** (nur das ist der echte Erfolgsnachweis). Klare Meldungen statt Rohtext: bei „Zugriff verweigert"/UAC-Ablehnung → „bitte bei der Windows-Nachfrage auf Ja klicken"; wenn die Elevation lief, der Ausschluss aber trotzdem fehlt → Hinweis auf den **Windows-Manipulationsschutz** samt Anleitung, den Ausschluss von Hand in der Windows-Sicherheit zu setzen – [src/main/defender.js](src/main/defender.js) `anwenden`/`startBefehl`. Merke: `Start-Process -Verb RunAs` niemals als Erfolg werten – immer am Ist-Zustand verifizieren; Tamper Protection kann `Add-MpPreference` selbst als Admin blockieren |
| 2026-09-24 | Nach gescheitertem Update bleibt die Grafik tot – GPU-Prozess crasht **auch im `gpu-aus`-Modus** (`code -2147483645`), FATAL-Schleife (Issue #107, degradierter NVIDIA-PC) | Der `gpu-aus`-Modus setzte nur `--disable-gpu` + `--disable-gpu-compositing`. Chromium startet dann aber **trotzdem noch einen GPU-Prozess für SwiftShader** (Software-GL), und genau der crasht auf dem kaputten Treiber – im Log verräterisch `hardwareBeschleunigt: true` trotz gpu-aus | `gpu-aus` schaltet jetzt zusätzlich **`--disable-software-rasterizer`** (kein SwiftShader → gar kein GPU-Prozess, reines CPU-Rendering) und `--disable-gpu-sandbox`. Mit Test – [src/main/grafik.js](src/main/grafik.js) `flaggenFuer('gpu-aus')`. Merke: `--disable-gpu` allein reicht auf kaputten Treibern nicht – SwiftShader läuft weiter im GPU-Prozess; erst `--disable-software-rasterizer` verhindert JEDEN GPU-Prozess |
| 2026-09-24 | „Update-Prüfung fehlgeschlagen: GitHub antwortet mit 403" – es kommen keine Updates/Sicherheits-Patches mehr an (Issue #105) | Der Update-Check lief über die **GitHub-API** (`api.github.com/repos/.../releases`). Unangemeldet ist die API auf **60 Anfragen/Stunde/IP** limitiert – ist das Kontingent aufgebraucht (häufiges Prüfen, geteilte IP), antwortet GitHub mit **403** und die Prüfung schlug komplett fehl | Update-Prüfung und -Download laufen im **stabilen Kanal jetzt über die Release-Download-URL** `…/releases/latest/download/latest.yml` bzw. `…/releases/download/<tag>/<exe>` – ein reiner Datei-Download, der **nicht** API-rate-limitiert ist. Die API wird nur noch für den Changelog (best-effort, 403 wird ignoriert) und den Test-Kanal (Vorabversionen) genutzt; 403 wird zudem in eine verständliche Meldung übersetzt. Der Installer wird weiterhin per SHA-512 aus latest.yml geprüft und nur vom offiziellen Release-Download geladen. Mit Tests (inkl. 403-Resilienz) – [src/main/updater-installer.js](src/main/updater-installer.js) (`_neuesteStabil`/`pruefen`/`_einspielen`). Merke: GitHub-**API** für Update-Checks meiden – die Release-**Download-URL** ist rate-limit-frei |
| 2026-09-23 | Julia schreibt „alles"/scheinbar doppelt in den Minecraft-Spielchat, v. a. beim Durchspielen (Nutzerwunsch: nur schreiben, wenn man es anmacht oder ihr jemand schreibt) | Sowohl echte Spieler-Fragen als auch die **autonome Durchspiel-Aufforderung** liefen über dasselbe `frage`-Event → `minecraftFrage` → `minecraft.antworten(antwort)`. Dadurch wurde auch Julias Durchspiel-Erzählung in den Spielchat geschrieben (wirkte wie „alles" bzw. doppelt zur Fenster-Anzeige) | Die autonome Durchspiel-Aufforderung wird als `auto: true` markiert; reiner Helfer `darfInChat(auto, mitreden)` – in den SPIELCHAT wird nur geschrieben, wenn es eine **echte Antwort auf einen Spieler** ist (`!auto`) **oder** der neue Schalter `minecraft.chat_mitreden` an ist. Julias Erzählung bleibt sonst nur im Fenster – [src/main/minecraft.js](src/main/minecraft.js) `darfInChat`, [src/main/main.js](src/main/main.js) `minecraftFrage`, Schalter in [src/renderer/mc.js](src/renderer/mc.js). Mit Test |
| 2026-09-23 | Leeres/totes Fenster BLEIBT auf einem degradierten NVIDIA-PC – jetzt per Log klar: **GPU-Prozess-Absturz** (`code -2147483645`) **trotz Software-Rendering**, v. a. direkt nach einem Update (`--updated`) → `[FATAL] GPU-Absturz trotz Software-Rendering` → Neustart-Schleife (Issue #101/#102) | Der `software`-Modus schaltet nur `app.disableHardwareAcceleration()`, setzt aber **kein `--disable-gpu`** – Electron startet also trotzdem einen GPU-Prozess (SwiftShader-Compositing), und genau der crasht auf dem kaputten Treiber. `software` war die **letzte** Leiter-Stufe → beim GPU-Crash dort blieb nur FATAL, es gab keine tiefere Stufe. (Der Update-Status wird NICHT zurückgesetzt – der Modus liegt im Datenordner und übersteht Updates; das war nicht die Ursache.) | Neue **letzte Stufe `gpu-aus`** (nach `software`) schaltet den GPU-Prozess **komplett ab** (`--disable-gpu` + `--disable-gpu-compositing`, plus `disableHardwareAcceleration`) – dann gibt es keinen GPU-Prozess mehr, der crashen könnte. Der GPU-Crash-Handler eskaliert jetzt `software` → `gpu-aus` (statt sofort FATAL); erst wenn auch `gpu-aus` crasht, FATAL. Mit Tests – [src/main/grafik.js](src/main/grafik.js) (`MODI`/`flaggenFuer`/`hardwareAus`), [src/main/startpruefung.js](src/main/startpruefung.js) (`startAbsichern`/`ausweichen`). Merke: „Software-Rendering" heißt bei Electron NICHT „kein GPU-Prozess" – dafür braucht es `--disable-gpu` |
| 2026-09-23 | Auto-Update kommt nicht an – „Lockfile / kein Zugriff", Update wird nicht eingespielt (Issue #97) | Der Virenscanner (Windows Defender) sperrt beim Installieren kurz Dateien im Programm-/Datenordner → der Installer scheitert an gesperrten/„in Benutzung"-Dateien; für den Nutzer sieht es aus, als würde das Update „nicht angezeigt"/nicht funktionieren | Neue, nutzerinitiierte **Defender-Ausnahme** für **nur Julias eigene** Ordner (Programm + Daten) und die EXE, per Admin/UAC: [src/main/defender.js](src/main/defender.js) (`ausschlussSkript`/`startBefehl` via `Start-Process -Verb RunAs -EncodedCommand`, `istAusgeschlossen`, `status`, `anwenden`), IPC `defender:status`/`defender:ausschliessen` in [src/main/main.js](src/main/main.js), Knopf in den Einstellungen (System) – [src/renderer/einstellungen.js](src/renderer/einstellungen.js), [src/renderer/einstellungen.html](src/renderer/einstellungen.html). Nur Julias eigene Pfade, nie aus KI-Eingaben; mit Tests ([test/defender.test.js](test/defender.test.js)). Merke: bei „Update in Benutzung/kein Zugriff" zuerst AV-Sperren als Ursache prüfen |
| 2026-09-20 | Mikro „immer noch das gleiche" – per Video-Frames analysiert: Julia hört ~6 s zu und springt dann OHNE Ergebnis (kein Text, keine Nachricht, kein Fehler) zurück auf „wartet" | Zwei Punkte: (1) Bug – bei LEEREM Whisper-Ergebnis fiel `.text ?? windowsText` nicht auf die Windows-Erkennung zurück (`??` greift nicht bei ""), also ging ein evtl. vorhandenes Windows-Resultat verloren. (2) Bei leerem Ergebnis passierte sichtbar nichts → wirkt wie „Mikro geht nicht", obwohl zugehört wurde (Ursache dann meist Mikro-Capture/Erkennung, nicht der Startpfad) | (1) `_whisperText` gibt bei leerem/Whitespace-Ergebnis `null` zurück → Fallback greift; (2) bei leerem Ergebnis klarer Hinweis im Chat „Ich habe nichts verstanden – Mikro testen / Sprechpause höher" (agent:hinweis `nichts_verstanden`) – [src/main/sprache.js](src/main/sprache.js), [src/main/main.js](src/main/main.js), [src/renderer/chat.js](src/renderer/chat.js). Erkenntnis: Videoframes per lokalem ffmpeg extrahieren, wenn Logs/Repro fehlen |
| 2026-09-20 | Mikro geht laut Nutzer „immer noch nicht" (Video geschickt, hier nicht abspielbar – keine sichere Ursache) | Nach zwei Fixes (Weckwort-Zustand, End-Stille-Timeout) weiter gemeldet; ohne Repro/Log nicht eindeutig eingrenzbar. Möglich: hängender Erkennungs-Prozess (dann bleibt `hoeren` gesetzt → Mikro dauerhaft tot) oder Erkennung liefert nichts | (1) Wächter in `zuhoeren`: schließt der PowerShell-Erkennungsprozess nicht, wird er nach 60 s hart beendet und der Zustand aufgeräumt (idempotent, `erledigt`-Flag) – so bleibt das Mikro nie dauerhaft blockiert. (2) Ausführliche MIKRO-Diagnose ins Start-Logbuch an jeder Verzweigung von `sprachUmschalten` (Zweig, Dauer, Textlänge, Fehler), damit der nächste Fehlversuch eindeutig zeigt, WO es hakt – [src/main/sprache.js](src/main/sprache.js), [src/main/main.js](src/main/main.js). Merke: bei nicht reproduzierbaren Laufzeit-Bugs erst Diagnose einbauen, statt weiter zu raten |
| 2026-09-20 | Mikro „bricht random ab" – die Aufnahme endet mitten im Satz (weiterhin gemeldet trotz früherem Weckwort-Fix) | Die Windows-Spracherkennung (`ERKENNEN`, auch für die Whisper-Aufnahme genutzt) hatte `EndSilenceTimeout = 1.0s` fest verdrahtet – eine normale Denkpause von ~1 s beendete die Aufnahme sofort (RecognizeMode Single) → wirkt wie zufälliger Abbruch | `EndSilenceTimeout` erhöht und einstellbar gemacht: env `JULIA_ENDESTILLE_MS` (locale-sicher in ms), Standard 1600 ms, Ambiguous +600 ms; neue Einstellung `sprache.pause_s` (0,5–5 s) mit Regler in den Sprach-Einstellungen – [src/main/sprache.js](src/main/sprache.js), [src/main/config.js](src/main/config.js), [src/main/main.js](src/main/main.js), [src/renderer/einstellungen.html](src/renderer/einstellungen.html). Merke: bei „Mikro schneidet ab" zuerst die End-Stille-Timeouts prüfen, nicht nur die Start-/Zustandslogik |
| 2026-09-19 | Minecraft: stirbt ständig „nichts zum Heilen" und startet die Durchspiel-Etappen bei 0 neu (aus dem Spiel-Logbuch, kam über Stunden nie durch, obwohl mehrfach Eisenrüstung/Diamanten) | Sie aß erst spät (Leben ≤8 Goldapfel, Hunger ≤16) → die Lebens-Regeneration (braucht Sättigung ≥18) lief oft gar nicht, also ging sie beim Graben/Erkunden mit halbem Leben k.o.; und die Jagd gab schon bei 32 Blöcken „keine Tiere" auf → kein Essen | Früher/proaktiver essen: Goldapfel schon ab Leben ≤10, normales Essen ab Hunger ≤18, damit Sättigung ≥18 bleibt und sich Leben von selbst regeneriert (reine, getestete `essenPlan`); Jagd-Suchradius 32→48 Blöcke – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-19 | Minecraft: „auf einzelne Spieler hören" ging nicht bedienbar – es fehlte die Oberfläche (Nutzerwunsch: Button ins Menü) | Backend war da (`minecraft.erlaubte` + Live-Sync über `config.on('aenderung')` → `setErlaubte`, und die In-Game-Befehle), aber es gab KEINE UI, um einzelne Namen einzutragen/zu entfernen | Editor im Minecraft-Tab: Namensfeld + „Hinzufügen" und die erlaubten Spieler als Chips mit ✕ (schreibt `minecraft.erlaubte` via `julia.setzen` → gilt sofort im Spiel) – [src/renderer/chat.html](src/renderer/chat.html), [src/renderer/mc.js](src/renderer/mc.js) |
| 2026-09-19 | Minecraft: Chat-Nachrichten im Tab lassen sich nicht abschalten (Nutzerwunsch: Button) | Die In-Game-Chatliste im Minecraft-Tab war immer sichtbar | Schalter „Chat-Nachrichten anzeigen" (Config `minecraft.chat_zeigen`, Standard an) blendet die Liste aus – [src/main/config.js](src/main/config.js), [src/renderer/chat.html](src/renderer/chat.html), [src/renderer/mc.js](src/renderer/mc.js) |
| 2026-09-19 | Minecraft: „laufen geht manchmal nicht" – klemmt (z. B. am Baumstamm) während der Wegfindung | Der Anti-Hänger gab bei aktiver Wegfindung zwar Sprung-Impulse, hatte aber KEIN „Aufgeben" für diesen Fall (das gab es nur beim Selbst-Laufen) – also sprang sie ewig gegen das Hindernis, ohne dass ein neuer Weg gesucht wurde | Wenn die Wegfindung trotz mehrerer Sprünge am selben Ort klemmt (`s.aufgeben` bei `wegsuche`), den Pathfinder den Weg **dynamisch neu berechnen** lassen (`setGoal(goal, true)`) – bricht die Aufgabe nicht ab, plant aber außenrum; zusätzlich als `haenger`-Eintrag mit Koordinaten geloggt – [src/main/minecraft.js](src/main/minecraft.js) `_antiHaenger` |
| 2026-09-19 | Minecraft: sollte schwimmen können (Nutzerwunsch) | Es gab keinen aktiven Schwimm-/Auftauch-Reflex – in tiefem Wasser konnte sie absaufen | Reflex `_schwimmen` im Tick: Kopf unter Wasser und Luft geht aus (<18) oder sie sinkt → „springen" = hochschwimmen; bei akutem Ertrinken (Luft ≤6) hat Auftauchen Vorrang vor allem anderen. Reine, getestete Entscheidung `schwimmHoch` – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-19 | Minecraft: verschwenderisch – baut sich JEDES Mal eine neue Werkbank/Ofen (im Logbuch dutzendfach `crafting_table hergestellt`) | `_herstellen` prüfte nur beim Craften ANDERER Dinge, ob eine Werkbank in der Nähe ist – beim Auftrag „Werkbank/Ofen herstellen" baute es aber immer eine neue, auch wenn schon eine im Inventar lag oder danebenstand | Sparsam-Kurzschluss in `_herstellen`: Für Blöcke aus `EINMAL_BLOECKE` (crafting_table, furnace) keine neue bauen, wenn schon eine im Inventar liegt oder eine ≤6 Blöcke entfernt steht – dann die vorhandene nutzen. Mit Test – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-19 | Minecraft: Herstellen scheitert wiederholt mit „Event updateSlot:0 did not fire within timeout of 20000ms" (aus dem Spiel-Logbuch) | `bot.craft` wartet auf die Server-Bestätigung des Slot-Updates (20 s). Auf den (oft „ohne Grund" trennenden/laggy) Servern blieb die Bestätigung aus – das Item war meist TROTZDEM hergestellt, aber der Craft warf, und die KI versuchte es 4× je 20 s erneut (80 s+ blockiert) | Craft je Schritt am Inventar verifizieren: nach einem Fehler kurz warten und prüfen, ob die Stückzahl des Ziel-Items gestiegen ist – wenn ja, als Erfolg werten und weiter, statt fälschlich zu scheitern (`_anzahlImInventar`) – [src/main/minecraft.js](src/main/minecraft.js) `_herstellen`. Merke: bei fremden Servern nie blind dem Timeout-Fehler trauen – am tatsächlichen Weltzustand (Inventar) messen |
| 2026-09-19 | Minecraft: „Verlassen" – Figur soll wegbleiben, nur bei echtem Crash/Kick zurückkommen (Nutzerwunsch) | `trennen()` setzte `this.bot=null` vor `quit()`, der `'end'`-Handler stieg früh aus – reconnectete also eigentlich nicht. Zur Sicherheit gegen jede Race explizit machen | Neues Flag `absichtlichWeg`: `trennen()` setzt es, `_wiederVerbinden()` bricht dann sofort ab, `verbinden()` setzt es wieder zurück. So joint sie nach „Verlassen" garantiert nicht von selbst – nur ein neuer Beitritt oder ein echter Crash/Kick verbindet wieder. Mit Test – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-19 | Minecraft: Julia konnte nicht mit dem Eimer umgehen (Nutzerwunsch) | Es gab kein Werkzeug/Handling für Wasser-/Lava-/Milcheimer | Neues Werkzeug `minecraft_eimer` + Methode `eimer(aktion)`: wasser_aufnehmen/lava_aufnehmen (Quelle in ≤4 Blöcken suchen, anvisieren, benutzen), wasser_setzen/lava_setzen (anvisierten Block leeren), milch (trinken – hebt Effekte auf). Reine, getestete Zuordnung `eimerPlan` (Aktion→Eimer/Quelle/Ergebnis, mit Aliassen) – [src/main/minecraft.js](src/main/minecraft.js). Platzieren ist best-effort (Ziel-Anvisieren ohne Ingame-Test) |
| 2026-09-19 | Minecraft: kämpft schlecht gegen Mobs, stirbt nachts (Nutzerwunsch: besser kämpfen) | Die Selbstverteidigung griff erst, wenn ein Mob **5 Blöcke** nah war, und nahm nur den **nächsten** – Skelette/Hexen beschossen sie also lange aus der Distanz, und in einer Gruppe fixierte sie den falschen Mob, während die anderen zuschlugen | Früher reagieren nach Mob-Typ (`gefahrReichweite`: Fernkämpfer 12, Creeper 9, Nahkämpfer 7 Blöcke) und in Reichweite die **gefährlichste** Bedrohung wählen (`bedrohWert`, Creeper→Schütze→Nahkämpfer, dann Nähe) statt nur der nächsten – [src/main/minecraft.js](src/main/minecraft.js) `_naheGefahr`. Mit Test. (Weiteres Feintuning – Schild/Blocken, Rückzug bei Übermacht – bei Bedarf per Minecraft-Logbuch.) |
| 2026-09-19 | Klick auf den neuen Tab (Video/„Schnitt") zeigt nichts | Der Bereich war als Nav-Knopf + `<section>` da, aber NICHT in `BEREICHE` (start.js) registriert; `ansichtSetzen` bricht bei unbekanntem Bereich sofort ab → der Klick tut nichts, die Ansicht bleibt versteckt | `video: 'ansichtVideo'` in `BEREICHE` ergänzt – [src/renderer/start.js](src/renderer/start.js). Tab zusätzlich in „Schnitt" (en „Cut") umbenannt. Regressions-Test [test/ansichten.test.js](test/ansichten.test.js) prüft: jeder Nav-Tab ist in BEREICHE UND hat eine `<section>`. Merke: neuen Tab immer an DREI Stellen verdrahten – Nav-Knopf (chat.html), `<section>` (chat.html) UND `BEREICHE` (start.js) |
| 2026-09-19 | Video-Schneiden „muss noch kommen" – der Tab war da, aber Schneiden tat nichts | ffmpeg (das eigentliche Programm) war weder mitgeliefert (`ffmpeg-static` ist keine Abhängigkeit) noch auf einem typischen Windows-PC im PATH → jeder Schnitt scheiterte still. Mitliefern wäre +80 MB pro Installer/Update und wegen der allow-scripts/lavamoat-Policy (blockiert das Download-Postinstall) fragil | ffmpeg wird jetzt **bei Bedarf geladen** (wie Whisper/Piper): geprüfte Windows-Binary aus dem ffmpeg-static-GitHub-Release, abgesichert gegen feste Größe + SHA-256, im Datenordner abgelegt; Auflösung überall „gesetzt → geladen → PATH". Knopf „ffmpeg herunterladen (~79 MB, einmalig)" im Video-Tab mit Fortschritt; danach offline nutzbar – [src/main/video-ffmpeg.js](src/main/video-ffmpeg.js), [src/main/main.js](src/main/main.js), [src/renderer/videotab.js](src/renderer/videotab.js). Merke: große Laufzeit-Binaries nicht in den Installer packen (Größe + lavamoat), sondern per geprüftem Bedarf-Download wie die Sprachmodelle |
| 2026-09-18 | Mikro per Hotkey UND das Weckwort „Hey Julia" gehen „ab und zu" nicht (zufällig, auch am Desktop) | `Sprache.vorleser()` zählt `vorleserAktiv` hoch, dekrementiert aber NUR in `fertig()`. Wird das satzweise Vorlesen abgebrochen (Fehler/neue Antwort), ohne dass `fertig()` lief, blieb der Zähler hängen → `sprichtGerade` DAUERHAFT true. Folge: der Mikro-Hotkey (`sprachUmschalten`) nahm den „spricht gerade → stumm"-Zweig statt zuzuhören, und das Weckwort wird nur bei `!sprichtGerade` scharfgestellt → beides tot bis Neustart. `stumm()` setzte den Zähler nicht zurück | `stumm()` setzt `vorleserAktiv` jetzt hart auf 0 (und meldet „Lautsprecher frei") – damit ist der stuck-Zustand weg und selbstheilend. Mit Test – [src/main/sprache.js](src/main/sprache.js). Merke: Zähler/Flags, die einen Bedien-Pfad gaten, müssen auf JEDEM Abbruchweg zurückgesetzt werden – am besten zentral im „Stopp" |
| 2026-09-18 | Google-Anmeldung: „Zugriff blockiert … Überprüfung nicht abgeschlossen" (Fehler 403 access_denied) | Die OAuth-App steht im Status „Testing"; ist das eigene Konto dort kein Testnutzer, liefert Google `access_denied`. Julia deutete das nur als „du hast abgelehnt" | Die `access_denied`-Meldung nennt jetzt BEIDE Fälle und den Fix (in der Google Cloud Console unter OAuth-Zustimmungsbildschirm → Testnutzer die eigene E-Mail eintragen bzw. App veröffentlichen) – [src/main/konten/google.js](src/main/konten/google.js) |
| 2026-09-18 | Minecraft-Aktivität erscheint im Gaming-Overlay und der schwebenden Blase (Nutzerwunsch: nur im Tab) | Agent-Ereignisse (`agent:text`/`werkzeug` …) und die Minecraft-Echos gingen über `anAlle` an ALLE Fenster – auch Overlay (`overlayFenster`) und Blase (`orbFenster`) | `anAgentAlle()` leitet Agent-Ereignisse im Minecraft-Kanal (`agent.aktiverKanal === 'minecraft'`) nur noch an die großen Fenster (`anChatFenster`: Chat/Einstellungen), nicht ins Overlay/die Blase; die Minecraft-Nutzer-/Fehler-Echos ebenso. Freigaben/Status bleiben unverändert sichtbar (Sicherheit) – [src/main/main.js](src/main/main.js) |
| 2026-09-18 | Auto-Update geht nicht mehr (gepackte Installation) | Das Repo wurde nach **Morni-Team/julia-ai** umgezogen, aber der Installer-Updater (`REPO`) und die `publish`-Config zeigten noch auf `MoinMornhart/julia-ai` – der Update-Check hing damit an GitHub-Redirects | `REPO` in [src/main/updater-installer.js](src/main/updater-installer.js) und `build.publish.owner` in [package.json](package.json) auf `Morni-Team` gestellt; Doku-Links in [src/renderer/einstellungen.js](src/renderer/einstellungen.js) mitgezogen; Test-Fixture angepasst. Merke: Nach einem Repo-Umzug IMMER Updater-`REPO`, publish-owner und hartkodierte GitHub-Links nachziehen |
| 2026-09-18 | Nach einem Server-Kick (z. B. periodischer Anti-Bot-/Timeout-Kick alle ~7,5 min) verbindet sich Minecraft nicht von selbst neu | `bot.on('end')` rief `_wiederVerbinden()` nur bei Verbindungsverlust (`!rauswurf`), nie nach einem Kick | Nach einem Kick jetzt neu verbinden, WENN es Sinn ergibt – reine, getestete Regel `kickWiederverbinden(grund)`: JA bei Timeout/Neustart/Spam-Schutz/unklar, NEIN bei Bann/Whitelist/Online-Mode/Version/Doppel-Login/Fliegen (wiederholt sich sofort). Backoff/Limit wie beim normalen Reconnect – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-18 | Outlook-Anmeldung scheitert mit kryptischem AADSTS90072 („… does not exist in tenant …") | Als Anwendungs-ID wurde versehentlich die ID des **Azure-Portals** (`c44b4083-…`, Microsoft-eigen) eingetragen; die ist auf einen Firmen-Tenant beschränkt und lehnt private Microsoft-Konten ab | Bekannte Microsoft-eigene App-IDs vorab mit klarer Erklärung ablehnen (Azure-Portal/Graph/CLI/PowerShell/Teams) und AADSTS90072 in `erklaeren()` übersetzen (eigene App-Registrierung mit „auch persönliche Microsoft-Konten" nötig) – [src/main/konten/outlook.js](src/main/konten/outlook.js) |
| 2026-09-18 | Ständige Windows-Benachrichtigungen, während Julia in Minecraft baut (Nutzerwunsch) | `minecraft.on('ereignis')` rief bei JEDEM Ereignis `melden(...)` – auch bei Routine wie „fertig gebaut"/„essen" → Push-Flut | Neue Einstellung `minecraft.benachrichtigen` ('alle'|'wichtige'|'keine', Standard 'wichtige'); reine, getestete Entscheidung `sollBenachrichtigen(art, modus)` mit fester Wichtig-Liste (getrennt/gestorben/gefahr/erreicht/rueckzug/niederlage) – Routine pusht nicht mehr, die Oberfläche zeigt aber weiter alles live. UI-Auswahl im Minecraft-Bereich – [src/main/minecraft.js](src/main/minecraft.js), [src/main/main.js](src/main/main.js), [src/main/config.js](src/main/config.js), [src/renderer/mc.js](src/renderer/mc.js). Merke: bei Config-`pruefen` einen eigenen `case` mit `return` NIE mitten in eine Boolean-Fall-Through-Kette setzen (bricht die darüberliegenden Fälle) |
| 2026-09-18 | VibeWorks steht doppelt in den MCP-Einstellungen (Issue #86) | Die Import-/Hinzufügen-Pfade (`mcp:import`, `mcp:hinzufuegen`) hängten neue Einträge OHNE Dedup an; wurde VibeWorks per Login UND per Import/JSON angelegt (jeweils neue Id), entstanden zwei „VibeWorks" | Getestete, reine Hilfe `ohneDoppelte(liste)` (Dedup nach Adresse/Befehl/Id, erster Treffer bleibt) in [src/main/mcp.js](src/main/mcp.js); angewandt beim Import und Hinzufügen sowie EINMALIG beim Start (bereinigt bestehende Doppel) – [src/main/main.js](src/main/main.js) |
| 2026-09-18 | Overlay/Titel zeigt wörtlich „{name}" (Regression aus dem Not-Füllungs-Fix v5.5.2) | Die per `sendSync`/Not-Füllung gelieferten ROHTEXTE enthalten den Platzhalter `{name}`, der sonst in `texteFuerRenderer` durch den Assistenten-Namen ersetzt wird – bei den Rohtexten fehlte diese Ersetzung, also stand „{name}" im Fenster | Der `standard-texte`-IPC-Handler ersetzt `{name}` jetzt selbst per `assistentName()` (fällt auf „Julia" zurück, wenn Config noch nicht geladen) für de und en; zusätzlich fängt die Preload-Not-Füllung einen stray `{name}` defensiv ab – [src/main/main.js](src/main/main.js), [src/preload/preload.js](src/preload/preload.js) |
| 2026-09-18 | Leeres Fenster + FATAL-Dialog BLEIBT (v5.5.1), obwohl die Oberfläche laut Screenshot voll gerendert ist (Issue #3/#54) – die WAHRE Ursache | Das neue Diagnose-Log war eindeutig: `standardtexte=0 notfuellung=0`. D. h. schon im PRELOAD kamen die mitgelieferten Texte leer an. Grund: `app.enableSandbox()` macht ALLE Preloads zu Chromium-Sandbox-Preloads, und dort ist **`require()` von lokalen Dateien nicht erlaubt** (nur `electron`/Built-ins). `require('../shared/texte')` (und `../shared/beschriftungen`) schlug im gepackten Build also **immer** fehl → `julia.standardTexte` leer → die „synchrone" Sofort-Beschriftung aus v5.3.1 lief nie → alle `[data-t]` leer → der Healthcheck hielt das Fenster für „leer" und eskalierte über die (auf `software` festhängende) Grafik-Leiter direkt zu FATAL – obwohl die App über den IPC-Pfad sichtbar funktionierte | (1) Texte SYNCHRON per `ipcRenderer.sendSync('standard-texte')` vom Hauptprozess holen (der darf die Datei lesen); Handler `ipcMain.on('standard-texte')` im Modul-Scope, vor dem ersten Fenster – [src/main/main.js](src/main/main.js), [src/preload/preload.js](src/preload/preload.js). (2) Not-Füllung im Preload **inline** (kein lokales `require` mehr). (3) „Beschriftungen leer" wird als eigener Typ `ui-texte-leer` gemeldet und löst **nie** die Grafik-Leiter/FATAL aus – nur echtes „Body/CSS fehlt" (`ui-healthcheck`) darf das. Merke: In einem **sandboxed Preload** NIEMALS lokale Dateien requiren – Daten per IPC (sendSync) holen; und ein „Texte leer"-Signal ist nie ein Grafikproblem |
| 2026-09-18 | Leeres Fenster BLEIBT auf dem PC des Nutzers – Neustart→Grafik-Leiter→FATAL-Schleife über ALLE Versionen (Issue #3/#54), obwohl schon v5.3.1 die Texte „synchron" setzen sollte | Das mitgeschickte Start-Logbuch war eindeutig: `texte=111/111 css=3 body=12 readyState=complete` und **kein** `RENDERER-FEHLER` – d. h. Body+CSS da, Skripte werfen nicht, aber **alle 111 Beschriftungen leer**. Also KEIN Grafikproblem (auch im `software`-Modus leer). Die generischen `[data-nav]`/`[data-t]`-Labels wurden **nur** über die fragile Renderer-Kette gefüllt (`standardTexteSetzen` → `julia.standardTexte` über die contextBridge); `init()`s Erfolgspfad rief `juliaTexteNach` gar nicht. Kam der Text-Satz über die contextBridge auf diesem PC leer an, blieb alles leer – und die Blank-UI-Heilung kurbelte sinnlos die Grafik-Leiter bis FATAL | **Not-Füllung im PRELOAD**, unabhängig von der Renderer-Kette: das Preload hat die Texte direkt (`require('../shared/texte')`) und füllt `[data-nav]`/`[data-t]` selbst – früh bei `DOMContentLoaded` und noch einmal im Healthcheck, BEVOR „leer" gemeldet/neu gestartet wird (greift die Füllung, kein FATAL). Reine, getestete Hilfe [src/shared/beschriftungen.js](src/shared/beschriftungen.js); Healthcheck mit mehr Diagnose (`notfuellung`/`standardtexte`/`julia`) in [src/preload/preload.js](src/preload/preload.js); zusätzlich `init()`-Erfolgspfad füllt die generischen Labels jetzt auch ([src/renderer/chat.js](src/renderer/chat.js)). Merke: kritische Oberflächen-Inhalte NIE allein von der contextBridge/Renderer-Kette abhängig machen – das Preload ist der verlässliche Rettungsanker |
| 2026-09-18 | Julia „stürzt einfach ab" / öffnet auf schwachen PCs nicht – teils ohne jede Spur (Issue #3/#7/#54, XXL-Robustheit) | Zwei Grundlücken: (1) Der **Hauptprozess** hatte **keinen** globalen Fangschirm – ein einziger unbehandelter Fehler (`uncaughtException`/`unhandledRejection`, z. B. aus einem Timer, einer fremden API oder einem Werkzeug) beendet die ganze App WORTLOS. (2) War der **Datenordner** gesperrt/schreibgeschützt, scheiterte der Start hart (FATAL), statt auszuweichen | (1) Neues Modul [src/main/absturzschutz.js](src/main/absturzschutz.js) hängt so früh wie möglich `uncaughtException`/`unhandledRejection` ein: **beim Start** ist ein solcher Fehler tödlich (klare Meldung + Logbuch statt stummem Verschwinden), **nach** dem Start läuft Julia weiter (Hintergrundfehler killt den Assistenten nicht) – mit Schleifenschutz gegen Spam; verdrahtet in [src/main/main.js](src/main/main.js) über `startFertig`. (2) `beschreibbarerOrdner()` wählt den ersten wirklich beschreibbaren Ordner (normal → Temp-Ersatz), sodass Julia auch bei gesperrtem appData öffnet – [src/main/startpruefung.js](src/main/startpruefung.js). Beides mit Tests ([test/absturzschutz.test.js](test/absturzschutz.test.js), [test/datenordner.test.js](test/datenordner.test.js)). Merke: langlaufende Prozesse (Hauptprozess) brauchen IMMER einen globalen Fangschirm; kritische Ressourcen (Datenordner) immer mit Fallback |
| 2026-09-17 | Auto-Update schlägt fehl – „Prozesse noch aktiv", neue Version kommt nicht an (Issue #77) | Der heruntergeladene Installer wird von Julia als **Kindprozess** gestartet; sein `taskkill /F /T /IM "Julia AI.exe"` killt per **/T (Prozess-BAUM)** auch den Installer selbst, sobald Julia beim Ausführen noch lebt (Race) → Update bricht mittendrin ab. Die GPU-/Renderer-Prozesse heißen ohnehin „Julia AI.exe" und werden schon per `/IM` (Name) erfasst – `/T` war unnötig und gefährlich | `/T` aus dem `taskkill` entfernt (nur noch `/F /IM`), zusätzlich die mitgelieferte `whisper-cli.exe` gezielt beenden (könnte eine Datei im Programmordner sperren) – [build/installer.nsh](build/installer.nsh). Wirkt ab dem NÄCHSTEN Installer; die kaputte Fassung braucht einmal eine manuelle Installation |
| 2026-09-17 | Leeres Fenster BLEIBT trotz v5.2.1 (Issue #3/#54) – der 20-s-Wächter sah die Beschriftungen weiter leer | Der v5.2.1-Verdacht (langsame Init/Race) war **falsch**: 20 s reichen locker. Wahre Ursache: **alle** Beschriftungen hängen an der IPC/Init-Kette (`init()` → `bereit` → `texteSetzen`). Schlägt/hängt in dieser Kette irgendetwas fehl (nicht nur langsam), werden die Beschriftungen **nie** gesetzt → Wächter meldet „leer" → Grafik-Rettungsleiter → FATAL-Neustart-Schleife | **Beschriftungen unabhängig von IPC/Init machen:** Der **Preload** lädt die mitgelieferten Standard-Texte per `require('../shared/texte')` und stellt sie **synchron** als `julia.standardTexte` bereit; `chat.js` setzt sie bei `DOMContentLoaded` sofort (`standardTexteSetzen`, füllt `[data-nav]`/`[data-t]` direkt) – das Fenster ist damit NIE leer, egal was in der Kette hakt. Der Hauptprozess spielt später den echten Satz darüber. Merke: Kritische Oberflächen-Inhalte nie allein von einem IPC abhängig machen – [src/preload/preload.js](src/preload/preload.js), [src/renderer/chat.js](src/renderer/chat.js) |
| 2026-09-17 | Leeres Hauptfenster auf einem langsamen/degradierten PC (NVIDIA, `treiber:null`) – FATAL-Neustart-Schleife „leer auch mit Software-Grafik" (Issue #3/#54); per Log bestätigt: kein Grafikproblem (Fenster lädt), sondern Beschriftungen leer | **Race:** `chat.js init()` holte die zwei Start-IPCs (`status`, `texte`) **nacheinander** (je bis 4 s Zeitlimit = bis 8 s), und die Beschriftungen füllt `texteSetzen()` erst nach `bereit` (= `init()`). Auf einem langsamen PC war `init()` erst ~11–13 s fertig – genau wenn der **Healthcheck (11 s)** „leer" meldete → Grafik-Rettungsleiter kurbelte alle Modi durch bis FATAL, obwohl Grafik nicht das Problem war. Auf schnellen PCs <2 s, daher nie sichtbar | Die zwei Start-IPCs **parallel** holen (`Promise.all`, max ~4 s statt ~8 s) – [src/renderer/chat.js](src/renderer/chat.js) `init`; Healthcheck-Zeit **11 s → 20 s** und **konkrete Diagnose** (Body-Kinder, leere/gesamt Beschriftungen, readyState, CSS) ins Log – [src/preload/preload.js](src/preload/preload.js). Merke: Blank-UI-Selbstheilung ist nur für echte Grafik-/Renderer-Abstürze; eine langsame Init darf sie nicht auslösen |
| 2026-09-17 | Release-Skript meldet „Installer nicht veröffentlicht: HTTP 500: Error saving asset …/Julia-AI-Setup.exe" – am Release hängen nur `.blockmap` + `latest.yml`, die `.exe` fehlt | Der Installer wurde lokal korrekt gebaut (`dist/Julia-AI-Setup.exe`), aber der **Upload der großen 114-MB-Datei** lief in einen **transienten GitHub-Fehler** (HTTP 500). Die kleinen Assets gingen durch, die `.exe` nicht | Die `.exe` einfach **nachladen**: `gh release upload <tag> dist/Julia-AI-Setup.exe --clobber` (ging beim zweiten Versuch durch). Wichtig: **kein** neues `npm run installer`/Release starten, solange der Nachupload läuft – ein neuer Build überschreibt `dist/Julia-AI-Setup.exe` mitten im Upload |
| 2026-09-17 | „Werkzeuge abschalten" bringt kein Tempo/Tokenersparnis; KI antwortet zu langsam, zu viele Tools (Issue #75/#76) | Abgeschaltete Werkzeuge (`werkzeuge_aus`) wurden nur bei der **Ausführung** blockiert (agent.js), aber weiter in `definitionen()`/`alle()` **ans Modell mitgeschickt** – die Beschreibungen kosteten also weiter Tokens/Zeit. „Ausschalten" hatte damit keinen spürbaren Effekt | `alle()` filtert jetzt abgeschaltete Werkzeuge **aus der Liste** (gehen gar nicht erst ans Modell): einzeln (`werkzeuge_aus`) **und** als ganze Kategorien (`kategorien_aus`, neue Bulk-Abschaltung mit UI). Kern-Werkzeuge ohne Kategorie bleiben immer an. Zusätzlich Standard-Denkaufwand `high`→`medium` (v5.0.1). Mit Tests – [src/main/werkzeuge.js](src/main/werkzeuge.js) `alle`/`KATEGORIEN` |
| 2026-09-17 | Android-APK-CI „baut mal, mal nicht" (Issue #6) | `julia-android` hatte **keine Lockdatei**; die CI nutzte `npm install`, das bei jedem Lauf die Abhängigkeiten **frisch auflöst** – ein neueres Transitiv-Paket (Expo/React-Native-Baum) konnte den Build sporadisch brechen, obwohl sich am Code nichts änderte | `package-lock.json` erzeugt (`npm install --package-lock-only`, in sync mit `package.json`) und **festgeschrieben**; CI von `npm install` auf **`npm ci`** umgestellt → exakt die gelockten Versionen, reproduzierbare Builds. Grüner Stand als Basis gelockt (frischer Build war grün) – [.github/workflows/android-apk.yml](.github/workflows/android-apk.yml), [julia-android/package-lock.json](julia-android/package-lock.json) |
| 2026-09-17 | Minecraft: bleibt beim selbst gesteuerten Vorlaufen an einem Hindernis hängen und springt endlos auf der Stelle (Issue #8) | Die Anti-Hänger-Hilfe gab bei Stillstand immer wieder nur einen Sprung-Impuls; kam die Figur trotzdem nicht weg, sprang sie ohne Ende gegen dasselbe Hindernis (Livelock), statt aufzuhören und neu zu planen | `haengerStatus` zählt jetzt aufeinanderfolgende Sprünge am selben Ort (`sp` im Anker) und meldet nach `maxSpruenge` (Standard 4) `aufgeben`; `_antiHaenger` stoppt daraufhin beim Selbst-Laufen (nicht bei aktiver Wegfindung) das Vorwärtsdrücken, sodass der nächste Schritt einen neuen Weg sucht. Mit Tests (Eskalation + Zähler-Reset bei Vorankommen) – [src/main/minecraft.js](src/main/minecraft.js) `haengerStatus`/`_antiHaenger` |
| 2026-09-17 | App startet, aber Oberfläche lädt nicht; Konsole: „Identifier 'mitZeitlimit' has already been declared" in `chat.js`, dann Folgefehler „imOverlay/$ is not defined" in start/verlauf/… (Issue #70) | Klassische `<script>`-Tags einer Seite teilen **einen** globalen Scope. `chat.js` deklarierte `const mitZeitlimit` und `const md`, die mit den globalen `function mitZeitlimit` (zeitlimit.js) bzw. `function md` (markdown.js) **kollidierten** → `chat.js` bricht beim Parsen ab → die von `chat.js` definierten Globals `$`/`imOverlay` fehlen allen übrigen Seitenskripten (Kaskade). Regression aus früherer Arbeit | Die kollidierenden `const`-Aliase in `chat.js` entfernt (globale Funktionen direkt nutzen). **Regressions-Test** [test/renderer-skripte.test.js](test/renderer-skripte.test.js) kompiliert jede eingebundene Skriptdatei und erkennt seitenweite Top-Level-Deklarations-Kollisionen – fand dabei sofort die zweite, maskierte `md`-Kollision. Merke: geteilte Renderer-Globals (aus `zeitlimit.js`/`markdown.js` etc.) nie mit `const` neu deklarieren – [src/renderer/chat.js](src/renderer/chat.js) |
| 2026-09-17 | Neues clippy-CI-Gate (`-D warnings`) schlägt fehl: 11 Lints in `julia-core` (Issue #65) | `cargo clippy --all-targets -- -D warnings` behandelt Warnungen als Fehler; überindentierte Doc-Listen (`//! *` mit weit eingerückten Folgezeilen) und `needless_range_loop` (`for i in 0..len` mit reinem Index-Zugriff) sind Standard-Lints | Doc-Listen einzeilig statt mit eingerückten Folgezeilen; Index-Schleifen auf Iteratoren umgestellt (`iter_mut().enumerate()`, verschachteltes `zip`) – auch in Tests, da clippy `--all-targets` prüft. Merke: neuen Rust-Code gleich clippy-sauber schreiben – [julia-core/](julia-core/) |
| 2026-09-17 | GitHub-Actions-Workflow bricht sofort ab: „This run likely failed because of a workflow file issue" (rust-core.yml, Issue #65) | Ein `run:`-Wert war ein **unquotierter** YAML-Skalar, der `Hinweis: cargo …` enthielt – das `: ` (Doppelpunkt+Leerzeichen) mitten im Skalar macht YAML zur Mapping-Interpretation und bricht das Parsing. Der Job startete gar nicht | Den betreffenden (unnötigen `cargo fmt`-)Schritt entfernt; generell in Workflow-`run:` kein `: ` in einem unquotierten Skalar – entweder Blockskalar `run: \|` nutzen oder den ganzen Wert quoten – [.github/workflows/rust-core.yml](.github/workflows/rust-core.yml) |
| 2026-09-17 | VibeWorks-Anmeldung „lädt ewig / nichts lädt", wenn der Server hängt (Issue #13) | `vibeworks.pruefen` rief `fetch` **ohne Zeitlimit** auf; antwortet der Server nie, kehrt der Aufruf nie zurück und die Login-Box friert ein | Abbruch-Zeitlimit (Standard 12 s) per `AbortController` + `Promise.race`; läuft die Zeit ab, wird die Anfrage abgebrochen und als klarer `netz`-Hinweis gemeldet statt zu hängen (mit Test) – [src/main/vibeworks.js](src/main/vibeworks.js) `pruefen` |
| 2026-09-17 | Repo-Check erneut (Issue #44): unsichtbare Zeichen in `hilfen.js` wieder roh im Quelltext + Prototype-Pollution-Muster in `config.set` | Der `\u`-Escape der Unsichtbar-Zeichen-Klasse aus v3.7.1 war zwischenzeitlich wieder durch rohe Zeichen ersetzt; und der Prototype-Pollution-Schutz war nur in `mischen`, nicht in der `set`-Schleife (auch wenn `pruefen` unbekannte Schlüssel ohnehin abweist) | `hilfen.js`-Regex wieder als `\u`-Escapes geschrieben (per Codepoint-Sweep verifiziert, 0 rohe Zeichen); in `config.set` gefährliche Schlüssel (`__proto__`/`constructor`/`prototype`) explizit abgewiesen (mit Test). Die übrigen ~96 Repo-Check-Befunde sind unverändert kontextbezogene False Positives (Pfad-Traversal mit eigenen Nutzerpfaden, OAuth-Loopback über http, GCM mit gesetztem Auth-Tag) – [src/main/hilfen.js](src/main/hilfen.js), [src/main/config.js](src/main/config.js) |
| 2026-09-17 | Repo-Check meldet „Geheimnisse" in Testdateien (Issue #42) | Beispiel-/Dummy-Tokens standen als Literale im Quelltext (`glpat-…`, `test-dummy-token`) und sahen für den Secret-Scanner wie echte Schlüssel aus | Die Test-Fixtures aus Teilen zusammensetzen (`['test','dummy','token'].join('-')`, `'glpat-' + '…'`), sodass kein Muster mehr wörtlich im Quelltext steht; Werte/Tests unverändert – [test/apps.test.js](test/apps.test.js), [test/token-erkennung.test.js](test/token-erkennung.test.js) |
| 2026-09-16 | Leeres Chat-Fenster mündet in eine Neustart-Schleife, obwohl die Einstellungen sauber rendern (Issue #55/#56/#3) | Der UI-Healthcheck feuerte nach 2,5 s und die Blank-UI-Selbstheilung startete ~0,6 s später neu – **bevor** das 8-s-Init-Zeitlimit im Renderer die Ersatz-Beschriftungen füllen konnte. Die beiden Sicherungen bekämpften sich → Neustart→Software-Rendering→FATAL, ohne dem Fallback eine Chance zu geben. Dass die Einstellungen rendern, zeigte: kein GPU-, sondern ein Start-/Init-Problem | Init-Zeitlimits auf 4 s je Abfrage gesenkt (Fallback ~8 s) und den Healthcheck auf **11 s** gelegt (nach dem Fallback), sodass die Ersatz-Beschriftungen zuerst greifen; zusätzlich lädt der Renderer die echten Texte im Hintergrund nach, falls der IPC nur langsam war – [src/renderer/chat.js](src/renderer/chat.js), [src/renderer/start.js](src/renderer/start.js), [src/preload/preload.js](src/preload/preload.js) |
| 2026-09-16 | Einstellungen lassen sich nicht mit dem Mausrad scrollen (Issue #56) | `body` hatte `overflow:hidden` **ohne feste Höhe** → der Body wuchs auf Inhaltshöhe, der scrollbare Bereich `.huelle` (flex:1, overflow-y:auto) hatte nichts zum Scrollen, und Inhalt unterhalb des Fensters war per Maus nicht erreichbar | `html{height:100%}` und `body{height:100vh}` gesetzt, damit `.huelle` auf Fensterhöhe begrenzt ist und das Scrollen greift – [src/renderer/einstellungen.css](src/renderer/einstellungen.css) |
| 2026-09-16 | Oberfläche bleibt leer auf bestimmten (NVIDIA-)PCs, obwohl der Healthcheck es nur meldet (Issue #3/#54) | Das Start-Logbuch zeigte: GPU aktiv (vendorId NVIDIA) aber `renderer/vendor/treiber = null` – die GPU ist degradiert und der Renderer hängt beim Aufbau, **ohne** ein GPU-/Renderer-Absturz-Event zu feuern. Deshalb griff die vorhandene Auto-Heilung (die nur auf Abstürze reagiert) nicht. Der Nutzer bestätigte: mit `--disable-gpu` kam das Bild zurück | Das Blank-UI-Signal des Healthchecks (`ui-healthcheck` „Beschriftungen leer") einmal in die Auto-Heilung eingehängt: ist Software-Rendering noch nicht aktiv, wird es gesetzt und **einmal** neu gestartet (`blankUiAbsichern`); ist es schon aktiv und trotzdem leer, klare Meldung statt Endlos-Neustart. Zusätzlich viel mehr Start-Logging (GPU-INFO markiert fehlende Treiber-Infos, Renderer „fertig geladen"/„reagiert nicht"/Ladefehler) – [src/main/startpruefung.js](src/main/startpruefung.js), [src/main/main.js](src/main/main.js) |
| 2026-09-16 | Werkzeug `einstellung_setzen` ließ die KI (mit Bestätigung) `anbieter`/`anbieter_url` setzen (Issue #51) | Ein vergifteter Chat hätte per einmaliger Bestätigung die Anbieter-Adresse auf einen fremden Server umbiegen und so das Gespräch/Daten abfließen lassen können | `anbieter`/`anbieter_url` aus `EINSTELLUNG_GELB` entfernt → sie sind jetzt ROT (nur der Nutzer von Hand). Zugleich `design.`/`brainstorming.`/`memos.` als unkritische, mit Rückfrage änderbare Schalter ergänzt; mit Test abgesichert – [src/main/werkzeuge.js](src/main/werkzeuge.js) |
| 2026-09-16 | Minecraft: bleibt im Wasser mit einem Block über dem Kopf hängen (Issue #28, bekannter mineflayer-Fall) | Die Anti-Hänger-Hilfe griff nur bei `onGround`; im Wasser ist `onGround` immer false, also wurde ein Steckenbleiben dort nie erkannt und kein Impuls gegeben | `_antiHaenger` wirkt jetzt auch im Wasser (Gate über die reine, getestete `haengerAktiv`-Funktion); dort bedeutet der Impuls „hochschwimmen" und wird länger gehalten (`haengerDauer`), damit sie sicher aufsteigt – kein Block-Abbau – [src/main/minecraft.js](src/main/minecraft.js) |
| 2026-09-16 | Repo-Check/Semgrep (Issue #44): Prototype-Pollution-Muster in `mischen` und rohe unsichtbare Zeichen im Quelltext | `mischen` prüfte Schlüssel mit `k in standard` – das ist für `__proto__`/`constructor` über die Prototypenkette wahr, der Schlüssel wurde also nicht übersprungen (fragil); und `hilfen.js` hatte die Unsichtbar-Zeichen-Klasse mit echten unsichtbaren Zeichen im Code stehen | In `mischen` gefährliche Schlüssel explizit überspringen und auf `hasOwnProperty` statt `in` umstellen – [src/main/config.js](src/main/config.js); die Regex-Klasse in `hilfen.js` als `\u`-Escapes schreiben (verhaltensgleich, per Codepoint-Sweep verifiziert) – [src/main/hilfen.js](src/main/hilfen.js). Die übrigen 93 Befunde sind im Kontext einer lokalen Einzelnutzer-App False Positives (Pfad-Traversal mit eigenen Nutzerpfaden, OAuth-Loopback über http, `data:`-Icon ohne SRI, RegExp mit festen/escapten Werten, GCM mit gesetztem Auth-Tag) |
| 2026-09-16 | „Ich sehe keine Elemente, obwohl die Logs fehlerfrei sind" (Issue #26/#45) | Beim Start wartet das Fenster mit `await julia.status()`/`julia.texte()` **ohne Zeitlimit**; erst danach füllt start.js über `bereit.then` alle Beschriftungen. Antwortet ein Start-IPC nie (Hänger), löst `bereit` nie auf → Oberfläche bleibt ganz ohne Text/Bedienung, und ein Hänger wirft nichts, also steht nichts im Log | Start-IPCs bekommen ein Zeitlimit (`mitZeitlimit`, [src/renderer/zeitlimit.js](src/renderer/zeitlimit.js)); läuft es ab, wird die Oberfläche mit Ersatz sichtbar/bedienbar und der Hänger ins Start-Logbuch gemeldet; `bereit` löst jetzt immer auf; der Healthcheck erkennt zusätzlich leere Beschriftungen – [src/renderer/chat.js](src/renderer/chat.js), [src/renderer/start.js](src/renderer/start.js), [src/preload/preload.js](src/preload/preload.js) |
| 2026-09-16 | Fenster startet, aber Oberfläche bleibt leer/unstyled – Knöpfe & Layout fehlen (Issue #45) | Griff das CSS nicht bzw. wurde der Inhalt nicht aufgebaut, gab es keine Erkennung und keine Erholung | UI-Healthcheck im Preload: kurz nach dem Laden prüfen, ob Stylesheets und Body-Inhalt da sind; ist die Oberfläche leer, ins Logbuch melden (`ui-healthcheck`) und **einmal** neu laden (Selbstheilung, kein Loop dank `sessionStorage`-Merker) – [src/preload/preload.js](src/preload/preload.js) |
| 2026-09-16 | Zweiter Start bringt kein Fenster – „läuft bereits", aber nichts erscheint (Issue #45) | Der `second-instance`-Handler rief `chatZeigen` nur, **wenn `chatFenster` schon existierte**; lief Julia nur im Tray (kein/zerstörtes Fenster), passierte beim erneuten Start nichts | Immer `chatZeigen('chat')` – die Funktion legt das Fenster bei Bedarf neu an, stellt es wieder her und holt es nach vorn – [src/main/main.js](src/main/main.js) `second-instance` |
| 2026-09-16 | uuid-Sicherheitslücke GHSA-w5hq-g745-h8pq (moderat, Issue #43) | Transitiv über yggdrasil bzw. @azure/msal-node kam uuid < 11.1.1 herein; `npm audit fix --force` hätte mineflayer auf 1.4.0 heruntergebrochen | In `package.json` ein `overrides` auf `uuid: ^11.1.1` gesetzt (v11 liefert weiterhin CommonJS `v4`, daher kompatibel); danach `npm audit` = 0 Lücken, alle Tests grün – kein mineflayer-Downgrade |
| 2026-09-16 | „Oberfläche lädt nicht" ohne jede Spur – kein Fehler wird geloggt (Issue #3) | Unbehandelte JS-Fehler in einem Fenster (Renderer) landeten nirgends; ohne Log war nicht erkennbar, warum die UI leer blieb | Preload fängt `window.onerror` und `unhandledrejection` in jedem Fenster ab und meldet sie an den Hauptprozess; sie landen im Start-Logbuch als `RENDERER-FEHLER` (lokal) – [src/preload/preload.js](src/preload/preload.js) + [src/main/main.js](src/main/main.js) (`diagnose:rendererFehler`) |
| 2026-09-16 | Wiederholte Renderer-Abstürze nach dem Start – App bleibt mit totem Fenster hängen (Issue #41/#3) | Nur GPU-Abstürze und Renderer-Abstürze **im Startfenster** lösten die Selbstheilung aus; stürzte der Renderer wiederholt **danach** ab, wurde das nur geloggt | `beiRenderer` zählt jetzt echte Renderer-Abstürze auch nach dem Start und stellt ab der Schwelle auf Software-Rendering um und startet neu (wie bei GPU-Abstürzen) – [src/main/startpruefung.js](src/main/startpruefung.js) |
| 2026-09-16 | Shell-Befehle hängen und lassen sich nicht mehr stoppen (Issue #36) | Das Zeitlimit legte nur die KI fest (bis 1800 s); der Nutzer hatte keine feste Obergrenze, mit der ein hängender Befehl sicher abbricht | Einstellbares **Standard- und Maximal-Timeout** (`shell.timeout_s` / `shell.max_s`); das Maximum begrenzt jeden Befehl, auch wenn die KI mehr will – abgebrochen wird per `taskkill /T /F` – [src/main/werkzeuge.js](src/main/werkzeuge.js) (`shellTimeout`) + Einstellungen „Shell-Befehle" |
| 2026-09-16 | Modell ohne Bild-Unterstützung: derselbe Screenshot-Fehler wiederholt sich (Issue #34, z. B. „Vision is disabled for model 'mimo-v2.5'") | Bei einem Vision-Fehler kam nur eine Meldung; Julia schickte weiter Screenshots → immer wieder derselbe 400-Fehler | Julia **merkt sich** solche Modelle (`modelle_ohne_bild` in der Config), lässt bei ihnen Screenshots künftig weg (mit Hinweis „screenshot omitted" statt Bild) und **wiederholt** die fehlgeschlagene Runde einmal automatisch ohne Bild – [src/main/anbieter/openai.js](src/main/anbieter/openai.js) (`verlaufUmwandeln` `ohneBild`), [src/main/agent.js](src/main/agent.js) (`_bildlosMerken`) |
| 2026-09-16 | Semgrep: „github-actions-mutable-action-tag" in `.github/workflows/pages.yml` (Issue #33) | Die Actions waren an verschiebbare Tags (`@v4`, `@v5`, `@v3`) gebunden; ein Tag kann umgehängt werden (kompromittiertes Action-Repo) → im CI liefe anderer/böser Code | Jede Action auf ihren **festen Commit-SHA** gepinnt (mit `# vX`-Kommentar), SHAs vorher per `gh api repos/<action>/commits/<tag>` geholt (kein Raten) – [.github/workflows/pages.yml](.github/workflows/pages.yml) |
| 2026-09-16 | Installer überschreibt laufende Version nicht / Auto-Update scheitert – trotz #9-Fix (Issue #32) | Der sanfte `taskkill` in `customInit` reichte nicht; electron-builders **Standard-Prüfung** zeigte weiter den blockierenden „App kann nicht geschlossen werden – Retry"-Dialog, der auch nach dem Kill hängen blieb | Den Hook **`customCheckAppRunning`** überschrieben (kein Retry-Dialog mehr) und **hart** beenden mit `taskkill /F /T` inkl. Kindprozessen (GPU/Renderer heißen auch „Julia AI.exe"), zwei Durchläufe – [build/installer.nsh](build/installer.nsh) |
| 2026-09-16 | Release-Skript legt Release an, hängt aber keinen Installer an; `npm run installer` bricht mit „duplicate dependency references" ab | Transienter electron-builder-Zustand beim Einsammeln der node_modules (nicht durch Quellcode) – der NSIS-Schritt lief nicht zu Ende (nur `.exe`, keine `.blockmap`/`latest.yml`) | `npm run installer` einfach erneut ausführen (lief beim zweiten Mal sauber), dann `gh release upload <tag> dist/Julia-AI-Setup.exe dist/Julia-AI-Setup.exe.blockmap dist/latest.yml --clobber`; vorher prüfen, dass `latest.yml` die richtige Version trägt |
| 2026-09-16 | Minecraft: Bot friert ein (verbunden, aber keine Reaktion) und macht nicht weiter | Automatisches Wiederverbinden griff nur bei echtem Verbindungsabbruch, nicht bei einem stillen Einfrieren (Physics-Ticks bleiben aus) | Hänger-Wache mit eigenem Timer erkennt ausbleibende Ticks (>30 s) und erzwingt einen Relog; Zustand wird vorher als JSON ins Logbuch geschrieben – [src/main/minecraft.js](src/main/minecraft.js) `_haengerWache`/`_relog`/`haengerErkannt` |
| 2026-09-16 | Minecraft: bleibt an einer 1 Block hohen Kante hängen und kommt nicht voran | Beim Laufen (Wegfindung oder selbst vorwärts) fehlte bei Blockade ein Sprung-Impuls – die Figur klebte an der Stufe, statt hochzuspringen | Anti-Hänger: erkennt Stillstand trotz Laufwunsch (kaum Positionsänderung über ~0,6 s am Boden) und gibt einen kurzen Sprung-Impuls – [src/main/minecraft.js](src/main/minecraft.js) `_antiHaenger` / `haengerStatus` |
| 2026-09-16 | Installer/Update bricht ab, wenn Julia noch läuft (Issue #9) | Der Assistent-Installer (`oneClick:false`) beendet eine laufende Instanz nicht selbst → gesperrte Dateien → „Datei in Benutzung"-Fehler bei Installation **und** automatischem Update | Eigenes NSIS-Skript `customInit`/`customUnInit` beendet eine laufende Julia vorher (sanft, dann hart; Prozessname via `${PRODUCT_FILENAME}`) – [build/installer.nsh](build/installer.nsh), eingebunden über `nsis.include` in [package.json](package.json) |
| 2026-09-16 | Android-APK-Build (Issue #24) bricht ab: `color/splashscreen_background not found` bzw. Release-Schritt `unexpected EOF` | `expo prebuild` erzeugt die Splash-Farbe nicht immer, obwohl `splashscreen.xml` darauf verweist; und ein gerades `"` im `gh release --notes`-Text beendete die Bash-Zeichenkette vorzeitig | Nach dem prebuild die Farbe `splashscreen_background` garantiert in `values/colors.xml` anlegen; Release-Notiz in einfachen Anführungszeichen ohne eingebettetes `"` – [.github/workflows/android-apk.yml](.github/workflows/android-apk.yml) |
| 2026-09-15 | „KI crasht, weil keine Vision" bei eigenen Anbietern (Issue #18) | Text-Modell ohne Bild-Unterstützung liefert bei einem Screenshot einen kryptischen API-Fehler | `fehlerAus` erkennt Bild/Vision-Fehler und macht daraus einen klaren Hinweis (Modell mit Bild-Unterstützung wählen oder ohne Screenshots arbeiten) – [src/main/anbieter/openai.js](src/main/anbieter/openai.js) |
| 2026-09-15 | App startet mit schwarzem Fenster und crasht (Issue #7) | GPU-/Renderer-Absturz beim Start; Software-Rendering griff erst beim nächsten manuellen Start | Beim ersten Absturz im Startfenster sofort Software-Rendering setzen **und automatisch neu starten**; crasht es auch damit, klare Meldung statt Endlosschleife – [src/main/startpruefung.js](src/main/startpruefung.js) `gpuUeberwachen` |
| 2026-09-15 | Minecraft: bleibt am Abgrund hängen, bis sie stirbt | Gefahrenwache fror das Vorwärts-Gehen auch während der Wegfindung ein → Livelock am Rand | Wache greift nur noch beim selbstgesteuerten Vorlaufen; bei aktiver Wegfindung weicht der Pathfinder selbst aus – [src/main/minecraft.js](src/main/minecraft.js) `_gefahrWache` |
| 2026-09-15 | Minecraft: reißt beim Abbauen Truhen/Bauten ab | `_abbauen` grub jeden passenden Block im Umkreis, auch Wertvolles | Geschützte Blocktypen (Truhen, Öfen, Türen, Glas …) nur bei ausdrücklicher Nennung; kleinerer Suchradius – [src/main/minecraft.js](src/main/minecraft.js) `GESCHUETZT_ABBAU` |
| 2026-09-15 | GitHub-Pages-Workflow bricht mit „Resource not accessible by integration" ab | `actions/configure-pages@v5` mit `enablement: true` darf Pages nicht selbst anlegen | Pages einmalig per API aktivieren (`gh api -X POST repos/…/pages -f build_type=workflow`), dann `enablement` aus dem Workflow entfernen – [.github/workflows/pages.yml](.github/workflows/pages.yml) |
| 2026-09-15 | `git push` im Release-Skript abgelehnt („fetch first") | Remote hatte einen Commit voraus (parallel gepusht) | `git fetch` + `git rebase origin/main`, Tag mit `git tag -f` auf den neuen Commit verschieben, dann Commit **und** Tag pushen; danach GitHub-Release + Installer manuell nachholen |
| 2026-09-15 | „Kann v0.7.0 nicht installieren" | Installer ist **nicht signiert** → Windows SmartScreen/Defender blockt eine unbekannte App | Workaround: Datei entsperren (Eigenschaften → Zulassen) bzw. „Weitere Informationen → Trotzdem ausführen". **Dauerhaft nur mit Code-Signing-Zertifikat** (kostenpflichtig); der Build signiert dann automatisch, sobald ein Zertifikat via `CSC_LINK`/`CSC_KEY_PASSWORD` bereitsteht |
| — | GPU-Prozess stürzt schon beim Start in Serie ab („GPU process isn't usable. Goodbye.") | Chromium gibt den GPU-Prozess auf; ohne Abfang bleibt der Start schwarz/stumm | Erster GPU-Absturz im Startfenster setzt **sofort** den Software-Rendering-Merker für den nächsten Start; ab Schwelle wird ausgewichen und einmal gemeldet – [src/main/startpruefung.js](src/main/startpruefung.js) |
| — | Sich gegenseitig aufhebende Grafik-Flags lassen den GPU-Prozess abstürzen | widersprüchliche `--`-Flags beim Start | `flaggenPruefen` erkennt bekannte Konflikte und schaltet auf Software-Rendering – [src/main/startpruefung.js](src/main/startpruefung.js) |

### Vorlage für neue Einträge

```
| JJJJ-MM-TT | Kurzbeschreibung des Fehlers | Ursache in einem Satz | Fix + Datei/Commit |
```

Immer wenn ein realer Fehler behoben wird: **hier eintragen** (welcher Fehler, wie gefixt),
damit die Lösung dokumentiert ist und beim nächsten Mal sofort greift.

## Offene Punkte (Issues #3, #7 – GPU/Diagnose) & Roadmap

- [x] GPU/Treiber beim Start ins Logbuch schreiben ([src/main/main.js](src/main/main.js), `GPU-INFO`).
- [x] Fehler-Journal (diese Datei).
- [x] Startcrash (schwarzes Fenster) selbst heilen: erster Absturz → Software-Rendering + Auto-Neustart (#7).
- [x] Opt-in-Diagnose: bereinigten Crash-/Diagnose-Bericht auf Wunsch als Bug an VibeWork senden ([src/main/diagnose.js](src/main/diagnose.js) mit Scrubber – nie IP/Tokens/PII; Werkzeug `diagnose`, Einstellung `diagnose.senden`, Standard aus).
- [x] Reparatur-Start: `Julia AI.exe --reparatur` (bzw. `--software`/`--safe`) erzwingt Software-Grafik, falls die GPU beim Start crasht ([src/main/main.js](src/main/main.js)).
- [x] Wöchentliche, tokenschonende Selbstprüfung: schaut ohne KI ins Start-Logbuch nach Abstürzen; meldet nur mit Zustimmung (bereinigt) an VibeWork – [src/main/selbstpruefung.js](src/main/selbstpruefung.js).
- [x] KI-Setup-Box für geheime Werte (Issue #51, Teil B): Werkzeug `geheimnis_anfordern` – die KI bittet um einen geheimen Wert (Passwort/Token), eine Box im Chat holt ihn, der **Wert** fließt direkt vom Fenster in den verschlüsselten Speicher (IPC `geheimnis:eingabe`) und wird **nie** an den Agenten/die KI zurückgegeben (nur „hinterlegt/abgebrochen"), nichts vom Wert wird geloggt – [src/main/werkzeuge.js](src/main/werkzeuge.js), [src/main/main.js](src/main/main.js) (`geheimnisAnfordern`), [src/renderer/chat.js](src/renderer/chat.js).
- [x] Token sparen bei eigenen Anbietern (Issue #16, Teil 1): Auf der OpenAI-kompatiblen Seite werden nur noch die letzten paar Screenshots mitgeschickt (Standard 3), ältere fallen raus – analog zum serverseitigen Clearing der Anthropic-Seite. Tool-Paarung bleibt intakt (nur die Bild-Blöcke fallen weg, die Werkzeug-Ergebnis-Texte bleiben) – [src/main/anbieter/openai.js](src/main/anbieter/openai.js) `verlaufUmwandeln` (`bildBehalten`).
- [x] Verlauf kappen bei sehr langen Gesprächen (Issue #16, Teil 2): Vor jeder neuen Nutzer-Runde wird der Verlauf auf die letzten ~40 echten Nutzer-Runden gekürzt. Geschnitten wird IMMER am Anfang einer echten Nutzer-Nachricht (nie mitten in einer Werkzeug-Kette), sodass der Verlauf gültig bleibt (kein verwaistes tool_result) – [src/main/agent.js](src/main/agent.js) `verlaufKuerzen`. (Echtes Zusammenfassen alter Runden per KI könnte später folgen.)
- [x] Token-/Schlüssel-Erkennung (Issue #51): erkennt echte Zugangs-Tokens vieler Anbieter (Präfixe wie `ghp_`, `sk-`, `AKIA`, `AIza`, `vw_`, JWT …) plus generisch sehr lange, zufällige Schlüssel; blockt einen Token als Geheimnis-**Namen** (die KI sieht nur Namen). Wiederverwendbar für spätere KI-Setup-Boxen und die Diagnose-Bereinigung – [src/main/token-erkennung.js](src/main/token-erkennung.js), angewandt in [src/main/geheimnisse.js](src/main/geheimnisse.js).
- [x] VibeWorks-Anbindung per MCP (Issue #51/#13/#18/#21): Login-Box in den Einstellungen (API-Schlüssel `vw_…`, geprüft über `GET /api/mcp/rules`, genaue „nicht angemeldet"-Codes), Verbindung über den vorhandenen HTTP-MCP-Transport. Schlüssel liegt verschlüsselt im Tresor, nie in der config, nie für die KI lesbar; nur über die Oberfläche (kein KI-Werkzeug) – [src/main/vibeworks.js](src/main/vibeworks.js), IPC in [src/main/main.js](src/main/main.js), UI in [src/renderer/einstellungen.js](src/renderer/einstellungen.js).
- [x] CPU-Fresser sicher entlasten (Issue #19/#26): im Boost-Tab auf Nutzer-Klick die Priorität eines Prozesses auf Idle senken (umkehrbar), **kein** hartes Suspendieren (Deadlock-Gefahr), feste Sperrliste (System/Julia nie), **kein KI-Werkzeug** – [src/main/win/win.js](src/main/win/win.js) `prozessBremsen`/`darfBremsen`, IPC `boost:bremsen` in [src/main/main.js](src/main/main.js), UI in [src/renderer/boost.js](src/renderer/boost.js).
- [x] Ein-Klick-Grafik-Reparatur (Issue #55): Knopf in den Einstellungen (System) stellt auf Nutzer-Klick auf Software-Grafik um und startet neu (Rettungsanker gegen ein leeres Fenster – das, was `--disable-gpu` von Hand tat), umkehrbar. IPC `reparatur:status`/`reparatur:software` – [src/main/main.js](src/main/main.js), [src/renderer/einstellungen.js](src/renderer/einstellungen.js) `reparaturEinrichten`.
- [x] Grafik-Fallback-Leiter (Issue #57): Statt nur „an/aus" probiert die Selbstheilung bei einem leeren Fenster mehrere Grafik-Backends der Reihe nach – normal → ANGLE d3d9 → ANGLE OpenGL → **SwiftShader** (reine Software-GL) → Hardware-Beschleunigung ganz aus – und merkt sich die funktionierende Stufe. Kein „eigener Renderer" (Chromium ist die Engine); das ist der professionelle Electron-Weg – [src/main/grafik.js](src/main/grafik.js) (`MODI`/`flaggenFuer`/`naechster`), Marker/Anwendung in [src/main/startpruefung.js](src/main/startpruefung.js) (`grafikModus`) und [src/main/main.js](src/main/main.js).
- [x] Treiber-Hinweis statt Auto-Install (Issue #55): Ist die Grafik degradiert (keine Treiber-Infos), zeigt die Reparatur-Sektion einen Hinweis mit **Link zur offiziellen Treiberseite** des Herstellers (NVIDIA/AMD/Intel, per PCI-Vendor-ID). Julia installiert bewusst **nichts** selbst (falscher Treiber = PC lahm); nur die bekannten Hersteller-URLs werden geöffnet – [src/main/treiber.js](src/main/treiber.js) `treiberQuelle`.
- [x] Agenten-Rollen (Issue #58, Baustein 1, BETA): eigene, benannte Rollen mit einer Zusatz-Anweisung an den System-Prompt; die aktive Rolle prägt Julias Verhalten zusätzlich. BETA-gated (Standard aus, nur per ausgeschriebener Bestätigung, KI kann es nicht selbst einschalten), Rollen sind nicht KI-setzbar; Sicherheit/Ampel bleiben unberührt – Config `rollen`/`rolle_aktiv`/`beta.agenten`, [src/main/prompt.js](src/main/prompt.js) `rollenHinweis`, UI in [src/renderer/einstellungen.js](src/renderer/einstellungen.js) `rollenEinrichten`. - [x] Agenten-Delegation (Issue #58, Baustein 2, BETA): Werkzeug `rolle_fragen(rolle, aufgabe)` – Julia delegiert eine fokussierte Teilaufgabe an eine Rolle. Der Sub-Agent macht **einen** Modell-Aufruf **ohne Werkzeuge** (nur Nachdenken, kein PC-Zugriff → sicher) und liefert die Antwort zurück (als fremder Inhalt behandelt). Additiv gebaut (eigener One-Shot, ändert den Haupt-Chatpfad nicht); das Werkzeug wird nur angeboten, wenn BETA-Agenten an ist – [src/main/agent.js](src/main/agent.js) `unterAgent`, [src/main/werkzeuge.js](src/main/werkzeuge.js) `rolle_fragen`.
- [x] Agenten-Prüfer-Schleife (Issue #58, Baustein 3, BETA): Werkzeug `mit_pruefer(ersteller, pruefer, aufgabe, runden)` – eine Ersteller-Rolle macht einen Entwurf, eine Prüfer-Rolle kritisiert ihn gegen die Aufgabe, die Ersteller-Rolle bessert nach, bis der Prüfer „BESTANDEN" sagt oder die Runden (1–3, Standard 2) erreicht sind. Alle Sub-Aufrufe über `unterAgent` (ohne Werkzeuge/PC-Zugriff), nur bei eingeschalteten BETA-Agenten – [src/main/werkzeuge.js](src/main/werkzeuge.js) `mit_pruefer`. Damit ist die #58-Agenten-Struktur (Rollen → Delegation → Prüfer-Schleife) komplett.
- [x] Nativer Kern-Anfang `julia-core` (Issue #65): eigenes Rust-Workspace-Crate mit den realen, ressourcenschonenden Techniken – lineares SSM mit O(1)-State (`state.rs`), ternäre `{-1,0,+1}`-Gewichte (`ternary.rs`), zeilenweise gestreamte Gewichte für O(1)-RAM (`stream.rs`), Grammatik-Masking auf Logit-Ebene (`grammar.rs`), begrenztes Koexistenz-Gate (`safety.rs`). Nur std, per `cargo test`-CI grün ([.github/workflows/rust-core.yml](.github/workflows/rust-core.yml)). Ehrlich: getestete Mathematik-Grundlage, **kein** trainiertes Modell – Training (Gewichte, Rechenzeit) + native Brücke (napi-rs) in die BETA sind der nächste, kostenrelevante Schritt für MoinMornhart. Diagramm/Doku in [docs/eigene-ki.md](docs/eigene-ki.md).
- [x] VibeWorks-Geräte-Anmeldung (Issue #17/#13, RFC 8628): „Mit Konto anmelden (Gerät)" – Julia holt per `POST /api/mcp/device` selbst einen Code, öffnet die Verifizierungs-Seite, pollt `…/device/token` (behandelt `authorization_pending`/`slow_down`/`access_denied`/`expired_token`), holt den `access_token` einmalig ab und legt ihn verschlüsselt im Tresor ab + MCP-Server mit der gelieferten `mcp_url`. Der `device_code` bleibt im Hauptprozess, der Schlüssel ist nie AI-/UI-sichtbar; einmalige Inhaber-Freigabe als Grenze. Kern getestet – [src/main/vibeworks.js](src/main/vibeworks.js) (`geraetStart`/`geraetToken`/`geraetSchleife`), IPC `vibeworks:geraetStart`/`:geraetWarten` in [src/main/main.js](src/main/main.js), UI in [src/renderer/einstellungen.js](src/renderer/einstellungen.js).
- [x] Wissensgraph als strukturiertes Langzeit-Gedächtnis (Issue #89): kleiner Graph aus Entitäten (Typ + Beobachtungen) und Relationen (`von –art– zu`), rein lokal als JSON (tmp+rename, robustes Laden, Zugangsdaten-Sperre wie beim Gedächtnis). Werkzeuge `graph_merken`/`graph_abfragen`/`graph_entfernen` in der abschaltbaren Kategorie „Gedächtnis"; ein Kurz-Überblick des Graphen hängt im Laufzeit-Kontext (nur wenn nicht leer), Details holt die KI per `graph_abfragen`. Bewusst **kein** Vektor-Dienst/keine fremde Abhängigkeit (anders als das referenzierte boop-agent-Projekt, das nur getierte Vektor-Erinnerungen + eine Kraft-Graph-Visualisierung nutzt) – [src/main/wissensgraph.js](src/main/wissensgraph.js), [src/main/werkzeuge.js](src/main/werkzeuge.js), [src/main/prompt.js](src/main/prompt.js) `laufzeitKontext`. Offen als Folge-Schritt: Modus-/Rollen-Gating (Code-Modus → nur Dateien + Graph; normaler Chat → nur Gedächtnis-Info).
- [x] Update-Absicherung: Verifizieren, Wiederholen, Backup/Rollback (Issue #100): Nach einem Update prüft Julia, ob wirklich die neue Version läuft (reine, getestete `updateSchritt`); kam sie nicht an, wird der Installer **einmal automatisch wiederholt**, danach klare Meldung statt Endlos-Schleife. Der Installer der zuletzt funktionierenden Version wird als **Rollback-Backup** aufgehoben (`update-backup/`), `zurueckRollen()` startet ihn erneut (IPC `update:zurueckrollen`), und bei einem gescheiterten Update wird auf das Backup hingewiesen. Mit Tests ([test/updater-installer.test.js](test/updater-installer.test.js)) – [src/main/updater-installer.js](src/main/updater-installer.js). (Der häufigste „Update → Fenster bleibt leer"-Fall ist bereits durch die Grafik-Stufe `gpu-aus` abgefangen, #101.)
- [ ] Treiber-/Abhängigkeitsprüfung im Installer.
- [x] Minecraft schlauer & robuster + KI nebenbei (Issue #93): (1) **Kampf/Überleben** – bei Unterzahl/wenig Leben zieht sie sich zurück und regeneriert, statt dumm zu sterben (reine, getestete `rueckzugPlan`), und geht sparsam mit dem seltenen verzauberten Goldapfel um (nur im Notfall, `heilWahl`). (2) **Volle Rüstung craften – immer beste Stufe** (`ruestungCraftPlan` + Methode `_ruestungCraften`, Befehl `!rüste dich`): plant je Teil die beste erreichbare Stufe aus dem Vorrat und legt sie an. (3) **KI nebenbei** (`agent.nebenAntwort`): lockere In-Game-Fragen beantwortet ein leichter, werkzeugloser Nebenläufer mit kleinem Token-Budget – auch WÄHREND der Hauptagent beschäftigt ist (baut/kämpft), statt „bin beschäftigt". Bestes Werkzeug/Rüstung anlegen (`_ausruesten`) und die try/catch-gekapselte Reflex-Schleife bestanden schon. Mit Tests ([test/minecraft-kampf.test.js](test/minecraft-kampf.test.js)) – [src/main/minecraft.js](src/main/minecraft.js), [src/main/agent.js](src/main/agent.js), [src/main/main.js](src/main/main.js), [src/main/anbieter/openai.js](src/main/anbieter/openai.js).
- [x] Minecraft-Persönlichkeit & soziales Gedächtnis (Issue #94, BETA, Standard aus): neues Modul [src/main/minecraft-sozial.js](src/main/minecraft-sozial.js) merkt sich pro Spieler Ruf/Vertrauen/Genervtheit (persistiert als `sozial.json`), leitet daraus eine **Haltung** ab (warm/neutral/kühl/**ghosten**) und ist **skeptisch** bei unrealistischer Prahlerei/Admin-/Creative-Ansprüchen (`unglaubwuerdig`) sowie bei unmöglichem Item-Zuwachs (`cheatVerdacht`); Anti-Ausnutzung (`ausnutzung`), Privat-Nachricht-Erkennung, Antwort-Verzögerung. Gefärbt wird die Antwort über den Laufzeit-Kontext der **Neben-KI**; Persönlichkeit (`minecraft.persoenlichkeit`) und Verzögerung (`minecraft.sozial_verzoegern`) einstellbar, Haupt-Schalter `minecraft.sozial` (BETA, aus) mit klarem UI-Hinweis, dass Julia dann im Spiel auch mit anderen plaudert (nur im Spiel, nie am PC). Mit Tests ([test/minecraft-sozial.test.js](test/minecraft-sozial.test.js)) – [src/main/main.js](src/main/main.js), [src/main/config.js](src/main/config.js), [src/renderer/mc.js](src/renderer/mc.js), [src/renderer/chat.html](src/renderer/chat.html). Offen als Ausbau: vollautonomes /tell-Initiieren (Julia beginnt selbst Gespräche), „echte Geschichten“ aus dem eigenen Logbuch erzählen, feineres Stil-Spiegeln.
- [x] Minecraft-Grenzen, Budget & Ruhezeiten (Issue #98, BETA – erweitert #94): Julia hat jetzt **persönliche Grenzen** (`ignorierDauerMin`/`grenzePruefen`/`wirdIgnoriert`: wird jemand zu nervig, ignoriert sie ihn eine berechnete Weile und sagt das einmal), ein **Token-Budget fürs Plaudern** pro Tag (`budgetStatus`/`tokenSchaetzen`, `minecraft.token_limit`; bei Erreichen **Verabschiedung** und Ruhe – spart Tokens), **selbstgesetzte Ruhezeiten** (`istRuhezeit`, `minecraft.ruhe_von`/`ruhe_bis`: in dem Fenster ist sie im Spiel „offline") und **Base-Gedächtnis** (`baseMerken`/`naheBase`: Chunk-Koordinaten, winzig – für „deine Base ist in der Nähe", aber nur wenn's stimmt). Alles nur bei `minecraft.sozial` (BETA, aus). Mit Tests ([test/minecraft-sozial.test.js](test/minecraft-sozial.test.js)) – [src/main/minecraft-sozial.js](src/main/minecraft-sozial.js), [src/main/main.js](src/main/main.js) `minecraftFrage`, [src/main/config.js](src/main/config.js), UI in [src/renderer/mc.js](src/renderer/mc.js)/[chat.html](src/renderer/chat.html). Bauten respektieren ist bereits abgedeckt (GESCHUETZT_ABBAU/blocksCantBreak). Offen: autonomes Selbst-Anschreiben (/tell), Türen/Redstone bei Spielernähe nutzen, echtes „Spaß/Sarkasmus"-Verständnis.
- [x] Julia bedient echte Schnitt-/Animationsprogramme (Nutzerwunsch): Statt eines eingebauten ffmpeg-Schneide-Tabs steuert Julia die **echten** Editoren über Bildschirm + Klick/Tasten. Neues Wissensmodul [src/main/schnittprogramme.js](src/main/schnittprogramme.js) mit Kürzeln/Ablauf für Premiere Pro, DaVinci Resolve, CapCut, After Effects (Keyframe-Animation), VEGAS, Shotcut, Filmora und der Adobe Creative Cloud (Dynamic Link, Media Encoder). Werkzeug `videoschnitt` (programme/hilfe/oeffnen) in Kategorie „video"; Öffnen unabhängig vom Installationspfad über `win.appOeffnen` (Get-StartApps + shell:AppsFolder). Mit Tests ([test/schnittprogramme.test.js](test/schnittprogramme.test.js)). Der frühere **Schnitt-Tab** in der UI wurde entfernt (Nav/Section/videotab.js/BEREICHE/Texte/CSS raus); die ffmpeg-Agent-Werkzeuge (`video_schneiden` etc.) bleiben und laden ffmpeg jetzt bei Bedarf selbst nach (`ctx.ffmpegSicherstellen`).
- [ ] **Multitasking** – mehrere Aufgaben/Aufträge gleichzeitig bzw. parallel verwalten (vom Nutzer gewünscht).
- [x] Android baubereit + auf aktuellem SDK (Issue #88/#91): Abhängigkeiten auf **Expo SDK 57** aktualisiert (autoritativ via `expo install --fix`: expo ^57, react 19.2.3, react-native 0.86.3, expo-secure-store/-speech/-status-bar 57.x, async-storage 2.2.0, expo-system-ui ergänzt). Die kostenlose GitHub-Actions-CI (`android-apk.yml`, ohne EAS) baut die APK erfolgreich; installierbare Datei liegt unter dem Release `android-latest`. `expo prebuild` läuft sauber (Config-Plugin `withJuliaAccessibility` SDK-57-kompatibel), `expo-doctor` 21/21, `npm ci` synchron. Eingecheckten Gradle-Cache entfernt, `.gitignore` für prebuild-Artefakte ergänzt.
- [x] Android-Unterstützung – KI-gesteuerte Bedienung (Issue #6): Julia bedient das Handy jetzt selbst über den Bedienungshilfen-Dienst (ohne Root). **Tokensparend**: der rohe Elementbaum wird zu einer kurzen, nummerierten Auswahl nur bedienbarer/beschrifteter Elemente verdichtet (`src/bildschirm.js` `verdichten`/`alsText`), die KI antwortet mit **einem** Kommando aus einer kleinen Grammatik (`aktionLesen`: KLICK/TIPPE/SCROLL/ZURUECK/START/APPS/FERTIG). Jeder steuernde Schritt läuft einzeln über die Freigabe (Ampel-Prinzip). Reine Logik mit Tests ([test/android-bildschirm.test.js](test/android-bildschirm.test.js)); UI im Steuer-Tab ([julia-android/App.js](julia-android/App.js)). Offen als Ausbau: mehrschrittige Auto-Ausführung mit Sammel-Freigabe, Shizuku/Termux-Variante.
